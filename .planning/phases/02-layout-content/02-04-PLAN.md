---
phase: 02-layout-content
plan: "04"
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/pages/HomePage.tsx
  - src/pages/AboutPage.tsx
  - src/pages/ContactPage.tsx
  - src/pages/CaseStudyPage.tsx
  - src/components/home/ProjectCard.tsx
  - src/components/home/ProjectGrid.tsx
  - src/components/home/ProjectModal.tsx
  - src/components/layout/Navbar.tsx
  - src/components/layout/Footer.tsx
autonomous: false
requirements: [RESP-01, RESP-02, RESP-03, RESP-04, RESP-05]

must_haves:
  truths:
    - "Mobile (< 768px) shows single-column project grid, simplified nav labels, all tap targets >= 44px"
    - "Tablet (768px-1024px) shows adapted 2-column grid and nav that is usable"
    - "Desktop (> 1024px) shows full staggered 2-column grid with complete nav labels"
    - "All text sizes scale across breakpoints using clamp() or responsive prefixes"
    - "Interactive elements (buttons, links, nav items, form inputs) have min 44px tap targets on mobile"
  artifacts:
    - path: "src/components/home/ProjectCard.tsx"
      provides: "Touch-friendly project card with min 44px targets"
    - path: "src/components/layout/Navbar.tsx"
      provides: "Responsive nav with mobile-friendly tap targets"
    - path: "src/pages/CaseStudyPage.tsx"
      provides: "Case study page responsive at all breakpoints"
    - path: "src/pages/ContactPage.tsx"
      provides: "Contact form with min 44px touch targets"
  key_links:
    - from: "All pages"
      to: "Tailwind responsive utilities"
      via: "md: and lg: breakpoint prefixes"
      pattern: "md:|lg:"

user_setup: []
---

<objective>
Audit and fix responsive layouts across all pages at mobile (375px), tablet (768px), and desktop (1280px) breakpoints. Ensure single-column layout on mobile, adapted grid on tablet, full staggered grid on desktop. Verify all interactive elements meet 44px minimum touch target size on mobile.

Purpose: Ensure the portfolio works beautifully across all screen sizes for hiring managers viewing on phones, tablets, and desktops.
Output: Responsive-polished pages verified at all three breakpoints with touch-friendly targets.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-content/02-01-SUMMARY.md
@.planning/phases/02-layout-content/02-02-SUMMARY.md
@.planning/phases/02-layout-content/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive audit and fix for all pages</name>
  <files>src/pages/HomePage.tsx, src/pages/AboutPage.tsx, src/pages/ContactPage.tsx, src/pages/CaseStudyPage.tsx, src/components/home/ProjectCard.tsx, src/components/home/ProjectGrid.tsx, src/components/home/ProjectModal.tsx, src/components/layout/Navbar.tsx, src/components/layout/Footer.tsx</files>
  <action>
    Systematic responsive audit across all pages at three breakpoints. Start the dev server (`pnpm dev`) and use browser devtools to test at each viewport width.

    **Breakpoint targets:**
    - Mobile: 375px width (iPhone SE/13 mini)
    - Tablet: 768px width (iPad portrait)
    - Desktop: 1280px width (standard laptop)

    **Audit checklist for each page:**

    **1. Navbar (`Navbar.tsx`):**
    - Mobile: 4-col grid works at 375px. Verify text doesn't overflow. Nav labels use short form ("Work", "About", "Contact"). Each nav cell must be >= 44px tall for tap targets. Current `h-16` (64px) on mobile is fine.
    - Tablet: Labels transition to desktop form at `md:` (768px). Verify enough space.
    - Fix: If any nav cell is too small or text overflows, adjust font size or padding.

    **2. Home page — ProjectGrid (`ProjectGrid.tsx`):**
    - Mobile: Single-column (already has `md:hidden` / `hidden md:grid` split). Verify cards stack properly.
    - Tablet: 2-column grid starts at 768px. Check if `md:ml-[25%] md:w-[75%]` leaves enough space at 768px (25% = 192px for hero offset). If the grid feels cramped, consider using `lg:` (1024px) for the 2-column grid instead.
    - Desktop: Staggered 2-column with left offset and 1px divider.
    - Fix: If tablet looks cramped, change grid breakpoint from `md:` to `lg:`.

    **3. ProjectCard (`ProjectCard.tsx`):**
    - Mobile: Full-width card, the button element must be touch-friendly. Current `pb-12 pt-6` provides adequate touch area. Verify the entire card is tappable.
    - Ensure minimum 44px interactive area. The card is already a full-width `<button>` so the touch target is the entire card.

    **4. ProjectModal (`ProjectModal.tsx`):**
    - Mobile: Full-screen modal (overlay covers screen). Close button must be >= 44px. Current `h-16` (64px) header is fine.
    - Desktop: Left 25% overlay, right 75% panel.
    - Verify "View full case study" link has adequate touch target. Add `py-3 inline-block` if needed.

    **5. CaseStudyPage (`CaseStudyPage.tsx`):**
    - Mobile: Content should be full-width with `px-6`. Tags wrap correctly. Title uses `clamp()`.
    - Tablet: Content still single-column but with more padding.
    - Desktop: Content capped at `max-w-[720px]`.
    - Verify `pt-16 md:pt-[130px]` clears the navbar at both mobile and desktop heights.
    - Verify prose text sizes are readable at all viewports.

    **6. AboutPage (`AboutPage.tsx`):**
    - Mobile: Fixed hero becomes static (`relative` on mobile). Content flows below hero.
    - Desktop: Fixed hero left, scrolling content right (25%/75% split).
    - Verify prose content is readable. Check that the markdown-rendered sections have adequate spacing.

    **7. ContactPage (`ContactPage.tsx`):**
    - Mobile: Same fixed/static hero pattern as About. Form is full-width.
    - Submit button: ensure `min-h-[44px]` for touch targets. Add `py-3` if current height is less.
    - Input fields: ensure adequate height. Current `py-2` on inputs may be too small. Add `py-3` for mobile touch-friendliness.

    **8. Footer (`Footer.tsx`):**
    - Verify renders at all breakpoints without horizontal overflow.
    - Check text alignment on mobile.

    **Text scaling check (RESP-04):**
    - Verify all headings use `clamp()` or responsive prefixes
    - Body text: should be `text-[16px] md:text-[18px]` or `text-[18px] md:text-[20px]` patterns
    - Labels: consistent `text-[12px]` to `text-[14px]` range

    **Minimum touch target check (RESP-05):**
    For each interactive element, verify >= 44px effective tap area:
    - Navbar links: `h-16` (64px) on mobile = OK
    - ProjectCard button: full-width = OK
    - Modal close button: inside `h-16` header = OK
    - Contact form submit: add `min-h-[44px]` if not already
    - "View case study" link in modal: add `py-3 inline-block` to ensure 44px
    - Form inputs: add `min-h-[44px]` to input/textarea

    **Apply fixes as needed.** Only change what fails the audit. Don't restyle things that already work.
  </action>
  <verify>
    <automated>cd /Users/slugmacro/Desktop/projects/slug-portfolio && pnpm exec tsc --noEmit && echo "PASS"</automated>
  </verify>
  <done>All pages render correctly at 375px, 768px, and 1280px viewport widths. Mobile shows single-column grid and simplified nav. Desktop shows full staggered grid. All interactive elements have >= 44px touch targets. Text scales across breakpoints. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 implementation:
    1. Home page with real project data from markdown pipeline
    2. Case study pages at /case-studies/:slug with full markdown body rendering
    3. About page rendering from about.md markdown body
    4. Contact form with validation and Formspree integration
    5. Responsive layouts across mobile, tablet, and desktop
    6. All old data files (projects.ts, about.ts) removed
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` to start dev server at http://localhost:5173
    2. **Home page (/):**
       - Verify project cards show real data (Whales Market, Whales Predict, etc.)
       - Verify staggered 2-column grid on desktop
       - Click a project card — modal opens with project details
       - Click "View full case study →" link in modal — navigates to case study page
    3. **Case study page (/case-studies/whales-market):**
       - Verify title, tags, year, role metadata render
       - Verify markdown body renders with proper typography
       - Try an invalid slug (/case-studies/nonexistent) — should redirect to /
    4. **About page (/about):**
       - Verify content renders from markdown (Strengths and Philosophy sections)
       - Desktop: fixed hero left, scrolling content right
    5. **Contact page (/contact):**
       - Submit empty form — inline validation errors appear
       - Submit with invalid email — email validation error
       - Submit with valid data — shows "Sending..." then success/error feedback
    6. **Responsive:**
       - Resize browser to 375px width — single-column grid, mobile nav labels
       - Resize to 768px — adapted layout
       - Resize to 1280px — full desktop layout
       - Check all buttons/links are easily tappable on mobile
    7. **Navbar:**
       - "Case studies" active on / and /case-studies/* routes
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` — zero TypeScript errors
2. Dev server starts and all pages render without console errors
3. All five responsive requirements verified (RESP-01 through RESP-05)
4. User visual verification via checkpoint
</verification>

<success_criteria>
- All pages render at 375px, 768px, and 1280px without layout breaks
- Mobile shows single-column grid and simplified nav
- All interactive elements have >= 44px touch targets
- Text scales appropriately across breakpoints
- User approves visual quality via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-content/02-04-SUMMARY.md`
</output>
