---
phase: 02-layout-content
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/index.css
  - src/pages/HomePage.tsx
  - src/components/home/ProjectGrid.tsx
  - src/components/home/ProjectCard.tsx
  - src/data/projects.ts
autonomous: true
requirements: [LAYO-01, LAYO-03, LAYO-04, CONT-01, CONT-03]

must_haves:
  truths:
    - "Home page shows project cards with real data from markdown files (title, year, color from frontmatter)"
    - "Staggered 2-column grid renders correctly with projects from content loader"
    - "Fixed hero + scrolling grid layout works end-to-end with migrated data"
    - "Tailwind typography plugin is activated and prose class applies styles"
    - "HomePage scroll lock uses useScrollLock hook, not direct document.body writes"
  artifacts:
    - path: "src/pages/HomePage.tsx"
      provides: "Home page with loader data and useScrollLock"
      contains: "import { projects } from \"@/content/loader\""
    - path: "src/components/home/ProjectGrid.tsx"
      provides: "Project grid consuming ParsedProject data"
      contains: "import { projects } from \"@/content/loader\""
    - path: "src/components/home/ProjectCard.tsx"
      provides: "Project card consuming ProjectMeta type"
      contains: "import type { ProjectMeta } from \"@/lib/markdown\""
    - path: "src/index.css"
      provides: "Typography plugin activation"
      contains: "@plugin \"@tailwindcss/typography\""
  key_links:
    - from: "src/components/home/ProjectGrid.tsx"
      to: "src/content/loader.ts"
      via: "import { projects }"
      pattern: "import.*projects.*from.*@/content/loader"
    - from: "src/components/home/ProjectCard.tsx"
      to: "src/lib/markdown.ts"
      via: "import type { ProjectMeta }"
      pattern: "import.*ProjectMeta.*from.*@/lib/markdown"
    - from: "src/pages/HomePage.tsx"
      to: "src/hooks/useScrollLock.ts"
      via: "useScrollLock hook"
      pattern: "useScrollLock"

user_setup: []
---

<objective>
Install Phase 2 packages (react-markdown, remark-gfm, @tailwindcss/typography), activate the typography plugin, and migrate all home page components from `src/data/projects.ts` TypeScript constants to `src/content/loader.ts` markdown pipeline data. Replace direct `document.body.style.overflow` writes with the `useScrollLock` hook.

Purpose: Establish the rendering foundation for Phase 2 and ensure all home page components consume real markdown data through the Phase 1 content pipeline.
Output: Home page rendering real project data from markdown files, typography plugin ready for prose rendering, old data file removed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/markdown.ts:
```typescript
export const ProjectSchema = z.object({
  slug: z.string(),
  title: z.string(),
  subtitle: z.string(),
  year: z.string(),
  tags: z.array(z.string()),
  role: z.string(),
  description: z.string(),
  scope: z.array(z.string()),
  impact: z.array(z.string()),
  color: z.string(),
  coverImage: z.string().optional(),
});

export type ProjectMeta = z.infer<typeof ProjectSchema>;
export interface ParsedProject {
  meta: ProjectMeta;
  body: string;
}
```

From src/content/loader.ts:
```typescript
export const projects: ParsedProject[];
export function getProject(slug: string): ParsedProject | undefined;
export const about: ParsedAbout;
```

From src/hooks/useScrollLock.ts:
```typescript
export function useScrollLock(active: boolean): void;
```

From src/data/projects.ts (TO BE DELETED):
```typescript
export interface Project {
  slug: string; title: string; subtitle: string; year: string;
  tags: string[]; role: string; description: string;
  scope: string[]; impact: string[]; color: string;
}
export const projects: Project[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and activate typography plugin</name>
  <files>package.json, src/index.css</files>
  <action>
    1. Install Phase 2 dependencies:
       ```bash
       pnpm add react-markdown remark-gfm @tailwindcss/typography
       ```
       If `react-markdown` fails due to React 19 peer dep, retry with `--legacy-peer-deps`.

    2. Activate the typography plugin in `src/index.css`. Add the `@plugin` directive AFTER `@import "tailwindcss"` and BEFORE the `@font-face` declarations:
       ```css
       @import "tailwindcss";
       @plugin "@tailwindcss/typography";

       @font-face { ... }
       ```
       The `@plugin` MUST come after `@import "tailwindcss"` — placing it before will cause the plugin to not activate (Tailwind v4 processes directives in order).

    3. Verify the dev server starts without errors: `pnpm dev` should show no Tailwind or plugin errors.
  </action>
  <verify>
    <automated>cd /Users/slugmacro/Desktop/projects/slug-portfolio && pnpm exec tsc --noEmit && grep -q "@plugin.*typography" src/index.css && echo "PASS"</automated>
  </verify>
  <done>react-markdown, remark-gfm, and @tailwindcss/typography are in package.json dependencies. The `@plugin "@tailwindcss/typography"` directive exists in index.css after the tailwindcss import. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate home page components to content loader data</name>
  <files>src/pages/HomePage.tsx, src/components/home/ProjectGrid.tsx, src/components/home/ProjectCard.tsx, src/data/projects.ts</files>
  <action>
    Migrate all home page components from `src/data/projects.ts` to `src/content/loader.ts`. The key structural change: `projects` from the loader is `ParsedProject[]` where each item has `{ meta: ProjectMeta, body: string }`. All property accesses like `project.slug` must change to `project.meta.slug`.

    **ProjectCard.tsx:**
    - Change import: `import type { Project } from "@/data/projects"` -> `import type { ProjectMeta } from "@/lib/markdown"`
    - Change props interface: `project: Project` -> `project: ProjectMeta`
    - No property access changes needed (ProjectCard receives `ProjectMeta` directly, not `ParsedProject`)

    **ProjectGrid.tsx:**
    - Change import: `import { projects } from "@/data/projects"` -> `import { projects } from "@/content/loader"`
    - Update all `project.slug` references to `project.meta.slug`
    - Pass `project.meta` (not `project`) to `<ProjectCard>`: `project={project.meta}`
    - Update all `key` props: `key={project.slug}` -> `key={project.meta.slug}`
    - Update `onClick`: `onProjectClick(project.slug)` -> `onProjectClick(project.meta.slug)`

    **HomePage.tsx:**
    - Change import: `import { projects } from "@/data/projects"` -> `import { projects } from "@/content/loader"`
    - Update `activeProject` lookup: `projects.find((p) => p.slug === projectSlug)` -> `projects.find((p) => p.meta.slug === projectSlug)`
    - The `activeProject` is passed to `ProjectModal`. Since `ProjectModal` will be migrated in Plan 02, pass `activeProject?.meta ?? null` for now (or keep passing the full `ParsedProject` and update `ProjectModal` props temporarily). **Decision: pass `activeProject` as full `ParsedProject | null` and update ProjectModal's type in Plan 02.**
    - Wait — `ProjectModal` currently expects `Project | null`. After this migration, `activeProject` will be `ParsedProject | null`. To avoid breaking the build, we must update `ProjectModal.tsx` props type here too. Update `ProjectModal.tsx`:
      - Import: change `import type { Project } from "@/data/projects"` to `import type { ParsedProject } from "@/lib/markdown"`
      - Props: `project: Project | null` -> `project: ParsedProject | null`
      - All internal references: `project.title` -> `project.meta.title`, `project.description` -> `project.meta.description`, `project.role` -> `project.meta.role`, `project.tags` -> `project.meta.tags`, `project.color` -> `project.meta.color`, `project.scope` -> `project.meta.scope`, `project.impact` -> `project.meta.impact`
      - Update the `import { motion, AnimatePresence } from "framer-motion"` to `import { motion, AnimatePresence } from "motion/react"` (standardize import path per Phase 1 decision)
      - Replace `document.body.style.overflow` writes with `useScrollLock` hook: add `import { useScrollLock } from "@/hooks/useScrollLock"`, call `useScrollLock(!!project)`, and remove the manual `useEffect` that sets `document.body.style.overflow`
    - **HomePage scroll lock migration:** Replace the `useState(true)` + `useEffect` + `setTimeout(900ms)` pattern with `useScrollLock`. Since the original pattern auto-unlocks after 900ms for a loading animation effect, and there is no loading animation in Phase 2 yet (that's Phase 3), REMOVE the scroll lock on page load entirely. The `useScrollLock` is already handled by `ProjectModal` when a modal is open. Remove the `scrollLocked` state and the `useEffect` that writes `document.body.style.overflow`.

    **After all migrations verified:**
    - Delete `src/data/projects.ts` — it is no longer imported anywhere.

    **TypeScript verification:** Run `pnpm exec tsc --noEmit` after all changes. Fix any remaining type errors before committing.
  </action>
  <verify>
    <automated>cd /Users/slugmacro/Desktop/projects/slug-portfolio && pnpm exec tsc --noEmit && ! test -f src/data/projects.ts && echo "PASS"</automated>
  </verify>
  <done>ProjectGrid, ProjectCard, ProjectModal, and HomePage all import from `src/content/loader.ts` and `src/lib/markdown.ts`. No imports from `src/data/projects.ts` remain. The file `src/data/projects.ts` is deleted. ProjectModal uses `useScrollLock` hook and imports from `motion/react`. HomePage has no direct `document.body.style.overflow` writes. TypeScript compiles with zero errors.</done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` — zero TypeScript errors
2. `pnpm dev` — dev server starts without errors
3. No imports from `src/data/projects.ts` exist (file deleted)
4. `src/index.css` contains `@plugin "@tailwindcss/typography"` after `@import "tailwindcss"`
5. `grep -r "from.*@/data/projects" src/` returns no results
6. `grep -r "document.body.style.overflow" src/` returns no results (both HomePage and ProjectModal migrated to useScrollLock)
</verification>

<success_criteria>
- Home page renders project cards from markdown pipeline data
- Staggered 2-column grid works with migrated data (same visual output)
- Typography plugin installed and activated
- All direct scroll lock writes replaced with useScrollLock hook
- Old projects.ts data file deleted
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-content/02-01-SUMMARY.md`
</output>
