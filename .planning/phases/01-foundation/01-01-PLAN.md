---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/vite-env.d.ts
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - THEM-01
  - THEM-02
  - THEM-03
  - THEM-04
  - ARCH-01

must_haves:
  truths:
    - "Dev server starts without errors after all changes"
    - "Light theme colors render correctly — #fafafa bg, #e0e0e0 borders, #1a1a1a text, #4c48ff accent"
    - "Aeonik Pro font renders at weights 400, 500, and 700 with no system font fallback"
    - "TypeScript compilation has no errors including future *.md?raw imports"
    - "Border utility alone defaults to var(--color-border), not currentColor"
  artifacts:
    - path: "src/vite-env.d.ts"
      provides: "TypeScript declarations for Vite client types and *.md?raw module"
      contains: "declare module"
    - path: "src/index.css"
      provides: "Theme variables, font-face declarations, base layer resets"
      contains: "@theme"
    - path: "src/content/projects/"
      provides: "Directory for project markdown files"
    - path: "src/hooks/"
      provides: "Directory for custom hooks"
    - path: "src/components/common/"
      provides: "Directory for shared reusable components"
  key_links:
    - from: "src/vite-env.d.ts"
      to: "tsconfig.app.json"
      via: "TypeScript include path"
      pattern: "vite/client"
    - from: "src/index.css"
      to: "All components"
      via: "CSS custom properties via Tailwind @theme"
      pattern: "--color-"
---

<objective>
Verify and finalize the theme system, install content pipeline packages, create the folder structure, and add TypeScript declarations for markdown imports.

Purpose: Establishes the foundation that all other Phase 1 plans build on — correct theme, package availability, directory structure, and TypeScript support for the content pipeline.

Output: Verified theme, installed packages (gray-matter, zod, zod-matter), new directories (src/content/projects/, src/hooks/, src/components/common/), and vite-env.d.ts with *.md?raw declaration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing theme structure in src/index.css that Task 2 verifies -->
Current @theme block (src/index.css):
```css
@theme {
  --color-bg: #fafafa;
  --color-bg-card: #f5f5f5;
  --color-bg-nav: #fafafa;
  --color-text-primary: #1a1a1a;
  --color-text-secondary: #808080;
  --color-accent: #4c48ff;
  --color-border: #e0e0e0;
  --font-sans: "Aeonik Pro", ui-sans-serif, system-ui, -apple-system, sans-serif;
}
```

Current @layer base (src/index.css) — includes html, body, a, ::selection rules.
Font files confirmed present in public/fonts/: AeonikPro-Regular.woff2, AeonikPro-Medium.woff2, AeonikPro-Bold.woff2

vite-env.d.ts does NOT exist — must be created.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages, create directories, add TypeScript declarations</name>
  <files>package.json, pnpm-lock.yaml, src/vite-env.d.ts, src/content/projects/.gitkeep, src/hooks/.gitkeep, src/components/common/.gitkeep</files>
  <action>
1. Install the three content pipeline packages:
   ```
   pnpm add gray-matter zod zod-matter
   ```
   If `zod-matter` has a peer dependency conflict with zod 3.24.x, fall back to installing only `gray-matter` and `zod` (the loader will use manual `z.parse(matter(raw).data)` pattern instead).

   Check for `@types/gray-matter` — install as devDependency if available:
   ```
   pnpm add -D @types/gray-matter
   ```

2. Create the directory structure needed for the rest of Phase 1:
   - `src/content/projects/` — for markdown project files (Plan 03)
   - `src/hooks/` — for useScrollLock (Plan 02)
   - `src/components/common/` — for AnimatedSection (Plan 02)
   Do NOT add `.gitkeep` placeholder files — the subsequent plans will add real files to these directories immediately.

3. Create `src/vite-env.d.ts` with:
   ```typescript
   /// <reference types="vite/client" />

   declare module "*.md?raw" {
     const content: string;
     export default content;
   }
   ```
   This provides TypeScript support for Vite's `?raw` import suffix used by the content loader in Plan 03.

4. Verify `tsconfig.app.json` includes `src/` directory (it should by default via Vite scaffold). If `src/vite-env.d.ts` is not in the include path, add it.
  </action>
  <verify>
    <automated>cd /Users/slugmacro/Desktop/projects/slug-portfolio && pnpm exec tsc --noEmit 2>&1 | head -20 && echo "---" && ls src/content/projects/ src/hooks/ src/components/common/ src/vite-env.d.ts 2>&1</automated>
  </verify>
  <done>gray-matter, zod, and zod-matter (or zod only if peer conflict) are installed. Directories src/content/projects/, src/hooks/, src/components/common/ exist. src/vite-env.d.ts exists with *.md?raw declaration. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify theme and add border-color default</name>
  <files>src/index.css</files>
  <action>
1. The @theme values in src/index.css already match THEM-01 requirements (verified during research):
   - bg: #fafafa
   - borders: #e0e0e0
   - text: #1a1a1a
   - accent: #4c48ff
   Do NOT change these values. Verify they are still correct by reading the file.

2. Font files are confirmed present in public/fonts/ (AeonikPro-Regular.woff2, AeonikPro-Medium.woff2, AeonikPro-Bold.woff2). The @font-face declarations are correct. THEM-02 is satisfied.

3. Add a border-color default to `@layer base` to prevent Tailwind v4's `currentColor` default (Pitfall 4 from research). Add this rule inside the existing `@layer base {}` block:
   ```css
   *, *::before, *::after {
     border-color: var(--color-border);
   }
   ```
   This ensures `border` utility alone produces the correct subtle border color.

4. Run a grep across src/ for any hardcoded hex colors that bypass CSS variables (e.g., `bg-[#`, `text-[#`, `border-[#`). If found, document them but do NOT fix them in this task — they will be addressed in Phase 2 when components are migrated. The important thing is that the theme variables themselves are correct.

5. Verify `pnpm dev` starts without errors after changes.
  </action>
  <verify>
    <automated>cd /Users/slugmacro/Desktop/projects/slug-portfolio && grep -c "border-color: var(--color-border)" src/index.css && grep -c "#fafafa" src/index.css && grep -c "#4c48ff" src/index.css && pnpm exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>src/index.css @theme values match THEM-01. Border-color default added to @layer base. Font files verified present. No TypeScript errors. Dev server starts cleanly.</done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` — zero errors
2. `pnpm dev` starts without errors on port 5173
3. `ls src/content/projects/ src/hooks/ src/components/common/` — all directories exist
4. `cat src/vite-env.d.ts` — contains `declare module "*.md?raw"`
5. `grep "border-color" src/index.css` — shows var(--color-border) in @layer base
6. `grep "gray-matter\|zod" package.json` — packages listed in dependencies
</verification>

<success_criteria>
- Theme values in src/index.css match THEM-01 spec exactly
- Aeonik Pro font files exist and @font-face declarations are correct (THEM-02)
- Tailwind v4 @theme block is in place with CSS custom properties (THEM-03, THEM-04)
- Content pipeline packages installed (gray-matter, zod, zod-matter)
- Directory structure ready: src/content/projects/, src/hooks/, src/components/common/ (ARCH-01)
- TypeScript recognizes *.md?raw imports via vite-env.d.ts
- Border-color defaults to theme value, not currentColor
- Dev server runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
